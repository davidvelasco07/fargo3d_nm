Put on  scratch  of all nodes
(/scratch/frederic/mri) with number 0 the output
that we want to restart with -S 0.

Build MRI only (setup mri, no monitoring?) and
rename executable (see job file).
Check: blocks, MPICUDA, etc.
At this stage we must use -# PBS_ARRAYID -S 0 -p !!!!
-- 
Build MRICOR - rename, adapt job file:
At this stage we must use +# PBS_ARRAYID -S 1 -p !!!
